## Pulling the llama3.2:1b Model with Docker

To pull the `llama3.2:1b` model using Docker, run the following command:

```sh
docker exec -it ollama ollama pull llama3.2:1b
```

This command executes the `ollama pull` operation inside the running `ollama` container.
```